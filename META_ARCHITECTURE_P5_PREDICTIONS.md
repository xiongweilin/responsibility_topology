# Meta-Architecture P5-B — Pre-Literature Prediction Matrix

Status: **PREDICTIONS FROZEN BEFORE TARGETED PRIOR-ART INVESTIGATION**

Inputs available at this point are limited to the preregistered architecture definitions, the frozen P4/P4.5 artifacts, and the source-viability reading needed to establish that the two RAIB primary cases are reconstructible. No targeted literature comparison or final native-case adjudication has yet been performed.

Architectures:

```text
A0     = QX + QC current working architecture
A1     = QX + QC + unowned residual pool
A2     = Genesis + Currentness + Revision/Constitution
A_null = overlapping local problem families; no small global decomposition
A_D    = frozen neutral clusters D1-D5
```

## H_control — Helpston

### A0

```text
PredictedAbsorber:
  ordinary railway Rule Book compliance, human-factors/operational error analysis,
  local-instruction governance, and management-assurance practice.

PredictedResidualIfNotAbsorbed:
  only if the evidence shows that the operative system lacked a task-required
  distinction needed to tell permitted emergency override from routine use, or
  that multiple actors had to settle a shared determination not representable as
  ordinary line-management responsibility. Neither is expected from the viability read.

DecisionBeforeLiteratureReveal:
  ROUTE to native railway safety/rule/assurance literature first; presumptive KILL
  as a QX/QC architecture case if those mechanisms preserve all material facts.
  Low priority for QX/QC promotion.
```

### A1

```text
PredictedAbsorber:
  same native railway rule, training, local-instruction, and assurance mechanisms as A0.

PredictedResidualIfNotAbsorbed:
  any material governance/assurance distinction that survives the native account
  but does not satisfy QX or QC should remain UNOWNED rather than being promoted.

DecisionBeforeLiteratureReveal:
  ROUTE native first; if absorption fails, HOLD in the unowned residual pool with
  no new track. Low-to-medium research priority only if a concrete fact survives.
```

### A2

```text
PredictedAbsorber:
  ordinary rule-compliance, training, management-of-change/local-instruction,
  and assurance practice.

PredictedResidualIfNotAbsorbed:
  a Revision/Constitution question about how an emergency/out-of-course control
  became normalized in practice while the formal rule remained unchanged, and
  whether the local operating regime itself required revision rather than merely
  correction of one act.

DecisionBeforeLiteratureReveal:
  HOLD as a possible Revision/Constitution case, but KILL the architectural claim
  if native railway assurance and local-instruction governance already fully
  characterize the drift and corrective path. Medium priority.
```

### A_null

```text
PredictedAbsorber:
  native railway signalling rules, human factors, training, and management assurance.

PredictedResidualIfNotAbsorbed:
  none globally predicted; retain any surviving issue as a railway-specific local family.

DecisionBeforeLiteratureReveal:
  ROUTE locally. Do not manufacture a cross-domain residual merely because the case
  contains a historical rule/current practice contrast.
```

### A_D

```text
PredictedAbsorber:
  native rule-compliance and assurance mechanisms.

PredictedResidualIfNotAbsorbed:
  no D1-D5 fit is predicted a priori. D5 would become relevant only if the unresolved
  research question is corrective-action closure/return-to-use rather than why the
  unsafe local practice arose; D1 would require a genuine prior-evidence/current-use
  split rather than original noncompliance.

DecisionBeforeLiteratureReveal:
  ROUTE local first. Treat forced assignment to D1-D5 as a likely FP/OC failure unless
  the native reconstruction exposes the corresponding frozen failure distinction.
```

## H_unknown — Bookham Tunnel

### A0

```text
PredictedAbsorber:
  ordinary safe-system-of-work planning, information-transfer/configuration control,
  work-pack checking, safety-critical communication, and management assurance.

PredictedResidualIfNotAbsorbed:
  a QX-like residual only if the relevant planning/representation system genuinely
  lacked a task-required discriminator linking the intended work location to the
  correct line-blockage basis. A data-entry/transfer/checking failure with the needed
  distinction already available should be absorbed and must not be redescribed as QX.
  QC is not expected unless continued safe use actually requires a provisional shared
  determination that no ordinary authority/process can settle.

DecisionBeforeLiteratureReveal:
  ROUTE to native work-planning/information-management mechanisms first; HOLD QX only
  under the stronger missing-distinction condition. Medium priority because this is a
  direct test of QX false-positive control.
```

### A1

```text
PredictedAbsorber:
  same native work-planning, information-transfer, checking, and assurance mechanisms.

PredictedResidualIfNotAbsorbed:
  if a material cross-system or authority/assurance failure survives but is neither
  QX nor QC, retain it in the unowned pool rather than forcing ownership.

DecisionBeforeLiteratureReveal:
  ROUTE native first; HOLD an unowned residual only after strongest ordinary absorption
  fails. Medium priority because A1 should reduce owner-loss without creating a new track.
```

### A2

```text
PredictedAbsorber:
  ordinary safe-work-pack formation, data/configuration governance, checking, and
  assurance processes.

PredictedResidualIfNotAbsorbed:
  a Genesis/Revision distinction: the unsafe work basis may have been malformed during
  formation and then preserved through nominal checks, so the material question would
  concern provenance/formation discipline and the revision of the planning/assurance
  process rather than currentness of a previously correct basis.

DecisionBeforeLiteratureReveal:
  ADMIT provisionally to Genesis + Revision/Constitution, but KILL the added architectural
  value if native railway planning/assurance theory already supplies the same evidence,
  falsifier, and intervention path. Medium-high priority.
```

### A_null

```text
PredictedAbsorber:
  native railway safe-work planning, information-system transfer, checking, human
  factors, and assurance mechanisms.

PredictedResidualIfNotAbsorbed:
  none globally predicted; any surviving mechanism remains a local problem family.

DecisionBeforeLiteratureReveal:
  ROUTE local. A_null loses only if repeated local treatment demonstrably reinvents
  a stable cross-case evidence/kill/intervention protocol that a compact architecture
  captures with lower cost.
```

### A_D

```text
PredictedAbsorber:
  native safe-work-pack formation/checking and information-transfer mechanisms.

PredictedResidualIfNotAbsorbed:
  D2 is the only plausible frozen component if the material residual is genuinely about
  whether multiple actors can settle one shared work basis; otherwise no D1-D5 assignment
  is predicted. D3 would require missing operational selectivity, D1 later context drift,
  D4 preserved-review/current-force bifurcation, and D5 post-remediation closure.

DecisionBeforeLiteratureReveal:
  ROUTE native first; tentatively test D2 only if the native facts require a settlement/
  locality distinction. A forced D2 assignment when the failure is simply malformed input
  or ineffective checking counts as FP/MR.
```

## Freeze rule

The substantive predictions above may not be edited after targeted literature/native-outcome work begins. Later documents may quote them, mark them supported/refuted, or identify ambiguity, but may not retroactively improve their content.
