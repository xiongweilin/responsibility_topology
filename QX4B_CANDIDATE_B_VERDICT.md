# QX-4Bᵥ — Candidate B Aggregate Verdict

Status: **CANDIDATE B ELIMINATED**.

Formalization: **NO**.

Parent protocol: `QX_KERNEL_PREREGISTRATION.md` and `QX4B_DOMAIN_KILL_FREEZE.md`.

The earlier beta-decay/Pauli audit is retained as a preliminary negative control. The aggregate verdict below is based on the active preregistered three-domain cycle.

## 1. Active-domain results

```text
B-D1 incomplete model-based diagnosis / hidden interaction faults:
ELIMINATED
primary kills: B-K5, B-K8

B-D2 TLS 1.3 finite negotiation:
ELIMINATED
primary kill: B-K8
supporting pressure: B-K6, B-K5

B-D3 spacecraft fault-protection catalogues:
ELIMINATED
primary kills: B-K1, B-K4, B-K8
supporting pressure: B-K5, B-K6
```

No fourth domain may be added to rescue Candidate B.

## 2. Aggregate result

```text
Candidate B: ELIMINATED
```

The negative result is substantive because the three domains attack different escape routes.

### B-D1: genuine model-relative finite exhaustion is not enough

The incomplete-model diagnosis case grants Candidate B unusually strong premises:

```text
finite represented diagnosis class;
formal-model enumeration authority (E2);
model-relative exhaustiveness;
independent observational consistency criterion;
possible failure of every represented diagnosis.
```

Yet mature incomplete-model diagnosis already captures the evidential role. The result is an incomplete diagnostic model / hidden-interaction problem, not a distinct QX certificate.

### B-D2: even protocol-exact exhaustion is not enough

TLS gives the strongest enumeration authority in the cycle:

```text
E1 protocol-defined option structures;
concrete finite offers/support sets;
strong V -> H_V link;
independent compatibility/security criterion;
exact no-overlap exhaustion.
```

The correct conclusion remains:

```text
no acceptable negotiation exists under current offers/policies.
```

A specified abort/fail-closed response may correctly discharge the safety task. Therefore exact finite exhaustion does not imply representation inadequacy.

### B-D3: a finite operational catalogue need not be a finite universe

Spacecraft fault-management guidance supplies the opposite control. A finite identified/credible fault catalogue can be complete as an operational artifact while lacking authority to represent all possible task-relevant faults.

Thus:

```text
finite list
-/->
justified finite universe.
```

## 3. QX-D6 resolution

Original debt:

```text
QX-D6 determine whether finiteness contributes a genuine exhaustive-certificate result rather than decorative notation.
```

Resolution:

```text
RESOLVED NEGATIVELY FOR CANDIDATE B.
```

Finiteness can perform real exhaustive work inside a bounded protocol/model class. What the domain cycle fails to establish is the stronger step:

```text
finite represented-class exhaustion
->
task-relative distinction-space inadequacy.
```

That step either:

```text
collapses into mature incomplete-model/model-class reasoning;
reduces to ordinary option/negotiation failure;
or lacks a justified candidate-universe boundary.
```

Therefore Candidate B does not survive as an independent QX research object.

## 4. Enumeration-authority result

The active cycle supports a task-relative hierarchy:

```text
E1 protocol/grammar-defined set
E2 formally generated set relative to model assumptions
E3 institutionally governed operational catalogue
E4 analyst/designer list of known candidates
```

But no level automatically licenses a representation-inadequacy conclusion.

In particular:

```text
E1 can prove exact current option exhaustion without V inadequacy;
E2 can prove model-relative exhaustion while mature incomplete-model theory absorbs the result;
E3 can govern response coverage without certifying all possible world/task states;
E4 does not support semantic exhaustiveness by default.
```

## 5. Catch-all result

The cycle also freezes:

```text
Unknown-as-response
!=
Unknown-as-candidate.
```

A system may validly abort, defer, fail closed, escalate, or report `no diagnosis` without possessing an explanatory `unknown` hypothesis inside `H_V`.

Therefore an exhausted explanatory/negotiation class does not automatically defeat the broader relied-upon task.

## 6. Degrees of freedom permanently deleted

```text
finite enumeration
-/->
complete candidate universe;

complete finite exhaustion
-/->
representation inadequacy;

model-class rejection
-/->
QX certificate;

institutional catalogue authority
-/->
world/task-space completeness;

failure of explanatory identification
-/->
failure of every permitted task response.
```

The earlier beta-decay negative control additionally retains:

```text
retrospective finite list of famous hypotheses
-/->
historically complete H_V.
```

## 7. Candidate A remains separate and frozen

Candidate A is not strengthened by Candidate B's failure.

Preferred provisional name remains:

```text
pre-refinement discriminator-unavailability witness
```

Current evidence strength remains:

```text
one mechanism-specific Web-PKI survivor only.
```

No new Candidate-A domain is authorized by this verdict.

## 8. No formal opening

```text
Candidate B formalization: NO
Generic QX object: NOT EARNED
InsufficiencyCertificate: NOT EARNED
QX Lean: NO
```

Candidate B is now an archived negative-control lineage, not an object awaiting rescue.

## 9. Only legal QX synthesis step

The next QX step is restricted to:

```text
QX-5 Certificate-Provenance Non-Unification Audit
```

Default verdict:

```text
NO MERGE
```

QX-5 may compare Candidate A and Candidate B provenance structures. It may not create a generic theorem merely because both can be written as elimination over a current class.
