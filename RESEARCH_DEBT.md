# Responsibility Topology — Research Debt

Status: **active governance ledger.**

This file tracks unresolved research obligations. It is not a backlog of features and does not authorize formalization.

## Progress rule

The program uses the following research metric:

```text
progress
=
deleted freedom
+ resolved debt
+ independently surviving structure
```

Theorem count, constructor count, domain count, and repository activity are not progress metrics by themselves.

## Frozen / resolved debt

### Strict-L6 technical debt

Resolved at the frozen strict technical checkpoint:

```text
raw serialized selected runtime transition
-> Lean-computed restricted B0 contract
```

and explicit source-audited finite-domain interpretation layers.

No further technical bridge debt is open by default.

### QO standing/closure debt

Resolved negatively:

```text
unindexed A_K(e) default                     -> rejected
review -> standing shortcut                  -> rejected
use-difference -> standing shortcut          -> rejected
reviewability -> closure qualification       -> rejected
generic ChallengeStanding                    -> not earned
generic closure-defeater relation            -> not earned
QO-3 Lean phase                              -> blocked
```

The negative-control lineage remains evidence, not active theory construction.

## QX — Representation Inadequacy debt

### Resolved through QX-1/QX-2/QX-3

```text
QX-D1  define finite-system / distinction-space / relied-upon-task objects without old standing/closure vocabulary
       -> RESOLVED at QX-1 problem kernel

QX-D2  distinguish anomaly from a certificate that current representation is insufficient
       -> RESOLVED as a firewall / open provenance obligation, not as a positive generic object

QX-D3  attack unawareness, open-world/open-set, aliasing/aggregation, POMDP, CEGAR, model criticism
       -> RESOLVED at QX-2 prior-art kill

QX-D4  identify whether a residual remains after strongest-neighbor readings
       -> RESOLVED: narrow certificate-provenance residual remains

QX-D7  preregister domain falsification only after QX-D3/QX-D4 survive
       -> RESOLVED at QX-3; Candidate A domain cycle executed under frozen criteria
```

### Candidate A debt resolved by QX-4A

Original debt:

```text
QX-D5  if aliasing witness retained, state where accountability distinction comes from without assuming the correct finer representation
```

Current result:

```text
A-D1 Web PKI: SURVIVES WITH EXACT RESIDUAL
A-D2 737 MAX: ELIMINATED by existing-channel / policy-vs-representation distinction
A-D3 pulse oximetry: ELIMINATED by measurement/calibration localization
```

Candidate A therefore earns only:

```text
mechanism-specific pre-refinement certificate candidate
```

with the residual obligation stated as:

> Under what evidence conditions can a system establish that a specific task-relevant discriminator is unavailable at the relied-upon decision boundary before it possesses the later correct refinement, rather than merely unused, noisy, miscalibrated, or hidden elsewhere?

The Web-PKI survivor does **not** resolve this generically. It only proves that the question is nonempty in one source-backed mechanism.

Permanent negative constraints learned from QX-4A:

```text
post-refinement hindsight cannot supply earlier certificate provenance;
ignored existing signal != absent distinction;
measurement aliasing != representation inadequacy;
policy input vocabulary != full task-relevant system representation;
one survivor != generic theorem family.
```

### Open QX debt

```text
QX-D6  determine whether finiteness contributes a genuine exhaustive-certificate result rather than decorative notation
       -> OPEN; belongs to Candidate B / QX-4B

QX-D8  keep Lean closed until a non-definitional theorem/countermodel obligation is earned
       -> OPEN AS A GATE; current state remains NO

QX-D9  determine whether Candidate A's Web-PKI provenance mechanism survives a mechanism-distinct domain or has a stronger representation-independent justification
       -> OPEN; do not satisfy by adding rescue cases inside the completed QX-4A cycle
```

Candidate B is preregistered and may be tested only in a later, separate QX-4B cycle.

High-risk shortcut permanently prohibited:

```text
alpha_V(x)=alpha_V(y)
+
post-hoc Resp(x) != Resp(y)
-> representation inadequate
```

unless the responsibility-relevant difference and discriminator-unavailability evidence are independently justified.

## QC — Provisional Shared Determination debt

Original obligations:

```text
QC-D1  keep local determination, provisional shared determination, shared reliance, global truth, and final authority distinct;
QC-D2  separate agreement from semantic identity;
QC-D3  attack ordinary consensus/FBA/heterogeneous quorum explanations before proposing a new relation;
QC-D4  build a native failure corpus before a calculus;
QC-D5  include failures where shared abstraction erases real disagreement;
QC-D6  distinguish evidence ownership, decision authority, delegation, revalidation, and discharge;
QC-D7  determine which failure facts survive consensus + authority + semantic-translation decomposition;
QC-D8  keep Lean closed until a non-eliminable multi-agent responsibility structure survives.
```

### Resolved / sharpened through QC-3A

```text
QC-D1/QC-D2: firewalls established
QC-D3: broad prior-art kill executed
QC-D4: corpus seeded; QC-3A source-viability method successfully applied to a third source-backed negative control
QC-D8: Lean gate remains closed, now strengthened by zero source-backed positive residuals
```

QC-3A Web-PKI calibration result:

```text
source chain: SUFFICIENT FOR NATIVE DECOMPOSITION
ordinary decomposition: SUFFICIENT AT CURRENT EVIDENCE LEVEL
QC residual: NONE
classification: REJECTED AS QC EVIDENCE
```

The ordinary decomposition that currently preserves the material facts is:

```text
CA qualification
+ local root-program authority
+ credential lifecycle
+ staged distrust / revalidation
+ compatibility / interoperability
+ concrete actor-indexed ownership.
```

This case also resolves a cross-track methodological debt negatively:

```text
same source-backed event survives QX
-/->
same event supports QC.
```

### Open QC debt

```text
QC-D5  acquire source-backed F7-like cases where a shared abstraction appears to erase responsibility-relevant disagreement
       -> OPEN / HIGH PRIORITY

QC-D6  find source-backed cases where evidence ownership, authority, delegation, revalidation, and discharge cannot be preserved by ordinary role/contract models
       -> OPEN

QC-D7  compute native residuals after consensus + authority + semantic-translation + versioning/revocation/ownership decomposition
       -> OPEN; all source-backed cases so far have empty QC residual
```

F2–F8 remain source targets. QC-3A does not upgrade them by analogy.

Next permitted QC work:

```text
targeted source acquisition + native decomposition for F2–F8,
with F7 still the highest-priority falsifier.
```

Do not define `ProvisionalSharedReliance` and do not open Lean.

## Cross-track debt

Do not assume:

```text
RepresentationInsufficiency -> FalseSharedDetermination
```

as a theory law merely because QC may eventually contain examples of agreement caused by lossy shared abstraction.

The Web-PKI event now supplies a direct empirical negative control for that shortcut:

```text
QX Candidate A residual survives narrowly;
QC native residual = empty.
```

This is research evidence for track independence, not a formal theorem.

Any future QX/QC connection must first survive independently in each track.

## Restart / escalation rule

A debt item may justify a new formal object only if ordinary/native decomposition cannot express the relevant responsibility fact without loss.

When a candidate object appears, the first question is:

> **Can this be eliminated by a better representation of the existing domain vocabulary?**

Only a negative answer earned from evidence can reopen the formal gate.
