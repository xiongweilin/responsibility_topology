# Responsibility Topology — Research State

Status: **governance checkpoint after Strict-L6 and Q_open negative falsification.**

This file separates frozen technical evidence, archived negative-control research, and currently open evidence-gated research tracks. It is not a theorem manifest.

## 1. Frozen technical baseline

The technical consolidation remains frozen.

```text
Strict technical Level 6: PASS
scope: actual serialized selected runtime transition artifact
       -> Lean-computed restricted B0 contract
```

No new bridge fragment, runtime refinement, or cross-domain universal claim is authorized by this research-state update.

## 2. Q_open standing/closure lineage

The QO-1/QO-2 sequence is retained as a **negative-control lineage**.

It established durable methodological corrections:

```text
Admissible_K(item,use) replaces unindexed A_K(e) as the forward default;
review != standing;
different admissibility by use != standing;
reviewability != closure qualification;
gate review / preserved challenge != closure defeater;
syntactic/procedural scope != adequate localization.
```

The positive operationalization attempted in that lineage is rejected at the current evidence level:

```text
generic ChallengeStanding: NOT EARNED
generic closure-defeater relation: NOT EARNED
QO-3 formalization: BLOCKED
```

This result does **not** close the broader representation-inadequacy mother problem. It closes the standing/closure operationalization unless new evidence satisfies the restart conditions recorded in `Q_OPEN_POST_FALSIFICATION_SYNTHESIS.md`.

Do not rescue that lineage by introducing renamed substitutes such as:

```text
ChallengeStanding'
ClosureDefeater'
MetaAdmissibility
ReopenEntitlement'
```

without new non-eliminable evidence.

## 3. Parallel research architecture

The program now runs two independent evidence-gated tracks after the frozen Paper 1–3 and Strict-L6 baseline.

```text
Frozen Strict-L6
+
QX  Representation Inadequacy
+
QC  Provisional Shared Determination
```

There is no default execution dependency:

```text
QX -> QC        [not assumed]
QC -> QX        [not assumed]
```

The tracks may produce cases relevant to one another, but neither is a prerequisite for the other.

### QX — Representation Inadequacy

Mother question:

> **How can a finite system become entitled to suspect that its current space of distinctions is inadequate for a relied-upon responsibility task without already knowing the correct refinement?**

Current status:

```text
pre-formal research
Lean gate: CLOSED
```

The first obligation is to distinguish evidence for representation insufficiency from ordinary anomaly, model error, partial observability, open-set uncertainty, or an externally supplied finer representation.

### QC — Provisional Shared Determination

Mother question:

> **How can multiple finite systems become entitled to provisionally share a bounded determination across heterogeneous representations without a final arbiter?**

Current status:

```text
pre-formal scouting / failure-corpus work
Lean gate: CLOSED
```

The track must not collapse into ordinary consensus, quorum agreement, centralized authority, or semantic identity.

## 4. Shared formal gate

Neither QX nor QC is expected to formalize by default.

A Lean phase may open only after the relevant track has all of:

```text
stable objects;
strong prior-art elimination;
pre-registered kill protocol;
non-eliminable native structure in heterogeneous evidence;
a theorem/countermodel obligation that is not definition expansion.
```

## 5. Research progress metric

Progress is not measured by theorem count, constructor count, or number of domains surveyed.

Use:

```text
progress
=
deleted freedom
+ resolved research debt
+ independently surviving structure.
```

The controlling question after each research cycle is:

> **Which degrees of theoretical freedom were removed by the evidence?**

## 6. Current execution status

```text
Strict-L6 technical track: FROZEN / CLOSED
QO standing/closure operationalization: ARCHIVED NEGATIVE CONTROL
QX: ACTIVE, PRE-FORMAL
QC: ACTIVE, PRE-FORMAL
Q_close as old linear successor to Q_open: RETIRED
QX Lean: NO
QC Lean: NO
```

This state remains in force until a later evidence-gated checkpoint explicitly supersedes it.
