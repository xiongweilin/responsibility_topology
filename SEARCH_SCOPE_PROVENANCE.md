# Search-Scope Provenance for Research Freezes

Status: **GOVERNANCE CORRECTION / NO NEW RESEARCH CHECKPOINT**.

Formalization: **NO**.

This document re-audits existing freeze/park/dormancy decisions under one rule:

> **A recorded absence of positive residuals does not by itself establish that the search was sufficiently broad, sufficiently observable, or saturated. Search scope must be recorded before search closure can be claimed as an epistemic justification.**

This is a correction to freeze semantics, not a new QO/QX/QC theory.

## 1. Three state variables must remain separate

For every evidence-gated research track, distinguish:

```text
TheoryGate
SearchState
RealityHypothesis
```

Current meanings:

```text
TheoryGate = CLOSED
```

means only that the current evidence/theory record does not authorize constructing or formalizing a new object.

```text
SearchState = DORMANT / ACTIVE / SATURATED / EXTERNALLY-WAKEABLE / ...
```

records what the research process is doing. It requires its own provenance.

```text
RealityHypothesis = UNRESOLVED
```

means the record does not distinguish among at least:

```text
H1  the relevant phenomenon is genuinely rare or absent in reality;
H2  search coverage/capability was insufficient;
H3  the phenomenon exists but available sources cannot reconstruct it;
H4  the phenomenon exists but the current hypothesis/search vocabulary misses it;
H5  candidate cases exist but are correctly absorbed by stronger ordinary explanations.
```

Therefore:

```text
TheoryGate = CLOSED
-/-> RealityHypothesis = ABSENT

no positive residual found
-/-> search exhaustion

research dormancy
-/-> search saturation
```

## 2. Search exhaustion, saturation, and suspension are different

Freeze vocabulary must distinguish:

```text
SearchExhaustion
!= SearchSaturation
!= SearchSuspensionEntitlement
```

- **SearchExhaustion** requires a justified finite universe and actual exhaustion of that universe.
- **SearchSaturation** requires evidence that additional admissible search, across declared strategies/mechanism families, yields diminishing structural novelty rather than merely repeated sampling from one corner.
- **SearchSuspensionEntitlement** is a procedural permission to stop active search under bounded resources despite unresolved reality. It does not imply either exhaustion or saturation unless those have separately been established.

The existing repository has not established global SearchExhaustion or global SearchSaturation for QO, QX empirical wake, or QC.

## 3. Minimum search-scope record for any future search-closure claim

A future claim that active search is sufficiently complete to justify suspension on epistemic grounds must record at least:

```text
SearchQuestion
PositiveCriterion
Negative/AbsorptionCriteria
MechanismFamiliesSearched
MechanismFamiliesKnownButNotSearched
SourceClassesSearched
SourceAvailabilityFailures
SearchStrategiesUsed
Independent/AdversarialStrategiesUsed
Date/TimeBoundary
BudgetOrStoppingBudget
CasesScreened
CasesAuditedDeeply
CasesExcludedForInsufficientSources
CasesAbsorbedByOrdinaryExplanation
NovelResidualsBySearchBatch
MarginalNoveltyPattern
KnownBlindSpots
WakeConditions
```

A case count without these fields is not a saturation argument.

In particular, record four distinct possible inadequacies:

```text
CoverageInsufficiency
SourceInsufficiency
HypothesisInsufficiency
StrategyInsufficiency
```

and, when relevant:

```text
DiscriminatorInsufficiency
```

where the search may possess cases but lack a sufficiently discriminating audit protocol to separate a new residual from an ordinary explanation.

## 4. Re-audit of existing freezes

### 4.1 Papers 1–3 and Strict Technical Level 6

Type of freeze:

```text
DEFINED ARTIFACT / CONSTRUCTION-SCOPE FREEZE
```

These freezes do not depend on searching reality for positive counterexamples. They freeze an explicit manuscript/formal/bridge scope and its proved or audited claims.

Verdict under this audit:

```text
FREEZE JUSTIFICATION: UNAFFECTED
SEARCH-CLOSURE CLAIM: NOT APPLICABLE
REALITY HYPOTHESIS: NOT IMPLIED
```

The restricted Strict-L6 claim remains limited to the recorded serialized-transition -> Lean-owned B0 checker path. No statement about exhaustion of all runtime mechanisms or all external-domain behaviors follows.

### 4.2 QO standing/closure lineage

Recorded search/falsification scope includes:

```text
prior-art attack;
hostile renaming/regress/flooding attack;
preregistered native-domain elimination;
D1  Federal Rule of Evidence 103 / preserved error claim;
D2  pharmacovigilance / signal-management negative control;
D3  EPA participatory-science / use-indexing negative control;
final richer procedural decomposition of the D1 residual.
```

What this scope justifies:

```text
generic ChallengeStanding object: NOT EARNED;
generic closure-defeater object: NOT EARNED;
QO formalization: BLOCKED;
the tested standing/closure operationalization may remain archived as a negative control.
```

What was **not** recorded or established:

```text
systematic coverage of all institutions with preserved objections/review rights;
search saturation across legal, scientific, operational, and organizational mechanisms;
independent search-strategy replication;
a complete universe of possible gate-directed responsibility relations.
```

Therefore the correct interpretation is:

```text
candidate/program closure for the tested QO formulation: JUSTIFIED
search saturation: NOT ESTABLISHED
reality contains no other mechanism: NOT CLAIMED
RealityHypothesis: UNRESOLVED
```

QO remains an archived negative control because the specific generic construction was not earned, not because reality was exhaustively searched.

### 4.3 QX Candidate A

The Candidate-A cycle used a preregistered, deliberately heterogeneous three-domain set:

```text
A-D1  Web PKI / Symantec distrust
      role: possible pre-refinement provenance survivor;

A-D2  737 MAX / original MCAS
      role: hidden/existing-channel negative control;

A-D3  pulse oximetry / occult hypoxemia
      role: measurement/calibration negative control.
```

This domain set was designed to kill specific candidate freedoms, not to represent the universe of representation-inadequacy phenomena.

The cycle established:

```text
Candidate A broad aliasing reading: eliminated/narrowed;
ignored signal -> representation insufficiency: rejected;
measurement error -> distinction-space insufficiency: rejected;
one mechanism-specific Web-PKI provenance residual remains;
no fourth rescue domain is authorized inside the completed candidate cycle.
```

It did **not** establish:

```text
empirical saturation of mechanism-distinct Candidate-A-like cases;
absence of a second independent provenance mechanism in reality;
adequacy of the search vocabulary for discovering unknown mechanisms.
```

Current interpretation:

```text
Candidate-A cycle: COMPLETE
Candidate A: FROZEN / ONE MECHANISM-SPECIFIC SURVIVOR
E-WAKE empirical search saturation: NOT ESTABLISHED
RealityHypothesis: UNRESOLVED
```

### 4.4 QX Candidate B

The active Candidate-B kill protocol audited:

```text
B-D1  incomplete-model diagnosis / hidden interaction faults;
B-D2  TLS 1.3 finite negotiation;
B-D3  spacecraft fault/failure catalogues.
```

An earlier beta-decay/neutrino stress case was also retained as a pre-protocol negative control, but it was not part of the final frozen three-domain protocol.

The three active domains killed distinct freedoms:

```text
model-class exhaustion -> new QX object: rejected;
true finite protocol exhaustion -> representation inadequacy: rejected;
finite catalogue -> justified complete candidate universe: rejected.
```

This justifies:

```text
Candidate B: ELIMINATED as the preregistered QX candidate;
no fourth rescue domain in that candidate cycle.
```

It does **not** justify:

```text
all conceivable finite-exhaustion mechanisms have been searched;
reality contains no different finite-exhaustion phenomenon;
search saturation across all explanatory/model-class settings.
```

Current interpretation:

```text
Candidate-B candidate closure: JUSTIFIED
search saturation: NOT ESTABLISHED
RealityHypothesis: UNRESOLVED
```

### 4.5 QX T-WAKE

The theoretical-wake audit explicitly attacked the then-current candidate family through:

```text
finite-state observability / diagnosability;
active diagnosis;
interaction-level observational equivalence / POMDP-style equivalence;
model invalidation without a known replacement;
then survivor absorption against task-relative representation necessity / information-deficiency theory.
```

The result legitimately remains:

```text
T-WAKE: CLOSED FOR CURRENT CANDIDATE FAMILY
```

because the closure is family-relative and the audit itself states that no future theoretical wake is logically ruled out.

However the record does **not** establish exhaustive coverage of all possible theoretical mechanisms for search-independent representation self-indictment.

Therefore:

```text
current T-WAKE family closure: JUSTIFIED
all theoretical wake mechanisms exhausted: NOT ESTABLISHED
future materially different T-WAKE: logically possible
RealityHypothesis: UNRESOLVED
```

### 4.6 QX E-WAKE

Current repository language already says:

```text
E-WAKE: OPEN BUT NOT ACTIVELY SEARCHED
```

Under this audit, make the implication explicit:

```text
active empirical search saturation: NOT ESTABLISHED
SearchExhaustion: NOT ESTABLISHED
SearchSuspensionEntitlement based on saturation: NOT ESTABLISHED
```

The present dormancy is a governance/resource allocation decision after the completed candidate cycles, not an evidence-backed claim that the empirical wake space has been searched sufficiently.

Correct state:

```text
TheoryGate: CLOSED
SearchState: DORMANT / EXTERNALLY-WAKEABLE
SearchClosureEntitlement: NOT ESTABLISHED AS SATURATION
RealityHypothesis: UNRESOLVED
```

A naturally encountered mechanism-distinct evidence event may still enter E-WAKE without contradicting the old freeze.

### 4.7 QC

Recorded source-backed QC controls currently include:

```text
Stellar 2019 halt;
Mars Climate Orbiter;
Web PKI / Symantec distrust;
RPKI currentness / relying-party validation;
DNSSEC parent-DS / multi-provider revalidation.
```

The record also contains additional failure/source targets, but not all targets were acquired to source-backed audit depth.

Search/decomposition strategies actually used include:

```text
prior-art scouting against FBA / heterogeneous quorum;
judgment aggregation / belief merging;
semantic interoperability;
MaterialFactsFreeze;
domain-native decomposition;
RivalAdmissible / four-dimensional RivalFit;
SharedDeterminationExistenceGate;
source-rich F8 and F5 stress cases;
negative-control use of ordinary authority/currentness/revalidation models.
```

What this establishes:

```text
source-backed positive QC residuals currently recorded: 0;
Generic QC object: NOT EARNED;
ProvisionalSharedReliance: NOT EARNED;
QC Lean: NO;
several major false-positive routes are now ruled out by controls.
```

What is **not** established:

```text
coverage of all relevant mechanism families;
coverage of all organizations/protocols/authority topologies/time scales;
source adequacy for cases that may exist but are poorly documented;
independent multi-strategy saturation;
that F8/F5/F7 or the current vocabulary spans the true phenomenon class;
that marginal novelty has been measured across a declared search budget.
```

Therefore:

```text
QC TheoryGate: CLOSED
QC SearchState: EVIDENCE-LIMITED / DORMANT UNLESS A SOURCE-VIABLE EVENT APPEARS
QC SearchSaturation: NOT ESTABLISHED
QC SearchClosureEntitlement based on saturation: NOT ESTABLISHED
QC RealityHypothesis: UNRESOLVED
```

The statement "there is no obligation to collect another case for momentum" remains valid only as a governance rule against performative case accumulation. It must not be read as proof that the search is sufficiently saturated.

## 5. Current corrected freeze semantics

The repository-wide freeze should now be read as:

> **Under the research objects, sources, candidate families, and discrimination protocols actually audited so far, no current evidence authorizes further theory construction or formalization. The record does not establish that reality lacks positive cases, that empirical search is exhausted, or that QX/QC search is saturated.**

Equivalently:

```text
construction/formalization freeze: JUSTIFIED
search-saturation claim: NOT ESTABLISHED
ontological absence claim: NOT AUTHORIZED
RealityHypothesis: UNRESOLVED
```

This correction does not reopen QO, QX, or QC construction.

It only prevents an invalid inference from:

```text
no object earned under completed audits
```

to:

```text
sufficiently searched reality and found none.
```

## 6. Future freeze requirement

Any future research-stop/freeze document that relies on search sufficiency must include a `SearchScopeRecord` satisfying Section 3.

If no such record exists, the strongest allowed stop language is:

```text
TheoryGate: CLOSED under current evidence
SearchState: DORMANT or PAUSED for governance/resource reasons
SearchSaturation: UNASSESSED / NOT ESTABLISHED
RealityHypothesis: UNRESOLVED
```

It may not say or imply:

```text
searched enough;
no further cases likely exist;
empirical space exhausted;
negative result representative of reality;
no anomaly exists outside the audited scope.
```

## 7. No new research ladder

This file is repository-level governance provenance.

It does not create:

```text
QX-6;
QC-4;
a new Q_close theory;
SearchClosure predicate;
Lean formalization;
a claim that responsible search suspension has itself been theoretically solved.
```

The meta-problem remains open. This document only corrects what the existing freezes are entitled to mean.