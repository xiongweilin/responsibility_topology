# QX-4Aᵥ — Candidate A Aggregate Verdict

Status: **THREE-DOMAIN FALSIFICATION COMPLETE**.

Formalization: **NO**.

Parent protocol: `QX_KERNEL_PREREGISTRATION.md`.

Frozen domains: `QX4A_DOMAIN_FREEZE.md`.

Completed audits:

```text
A-D1  Web PKI / Symantec distrust     -> SURVIVES WITH EXACT RESIDUAL
A-D2  737 MAX / original MCAS         -> ELIMINATED
A-D3  pulse oximetry / occult hypoxemia -> ELIMINATED
```

No fourth rescue domain is authorized in this cycle.

## 1. What survived

Only A-D1 survives, and only under the narrow task fixed in its audit:

> **A contemporaneous, pre-refinement record can establish that a relied-upon decision boundary lacks a task-relevant discriminator without already identifying the correct finer representation, when the absence of the discriminator is independently evidenced and the task pressure for selective treatment is independently recorded.**

In the Web-PKI case the survivor mechanism is specifically:

```text
historical validation-provenance class
not recoverably discriminable at the client trust boundary
+
independently recorded security / compatibility-interoperability pressure
```

The safe conclusion is about unavailable **selective provenance-sensitive remediation**.

It is not:

```text
no safe action exists;
all current actions fail;
the correct refinement is known;
revision is authorized;
all representation insufficiency has this form.
```

## 2. What the negative controls killed

### 737 MAX

The broader Candidate A reading is killed when a distinguishing channel already exists in the task-relevant system but the failed policy/control law does not use it.

Frozen negative rule:

```text
policy input vocabulary
!=
full task-relevant system representation.
```

Therefore:

```text
ignored existing signal
-/->
representation insufficiency.
```

### Pulse oximetry

The broader Candidate A reading is killed when the aliasing is adequately explained by measurement/calibration error in an observation channel.

Frozen negative rule:

```text
same displayed measurement
+
different underlying state / response need
-/->
representation-insufficiency certificate.
```

Therefore measurement error, calibration error, ordinary observation noise, and available confirmatory channels must be eliminated before QX gets any residual.

## 3. Candidate A exact verdict

```text
Candidate A:
SURVIVES WITH EXACT RESIDUAL
```

Promotion level:

```text
MECHANISM-SPECIFIC CERTIFICATE CANDIDATE ONLY
```

Generic object:

```text
NOT EARNED
```

Generic theorem family:

```text
NOT EARNED
```

Lean gate:

```text
CLOSED
```

This follows the preregistered QX-3 rule: one surviving source-backed domain can justify at most a mechanism-specific certificate candidate. A later formal opening still requires either mechanism-distinct survival or a stronger independent argument that the certificate source is representation-independent in the relevant sense.

## 4. The surviving research residual is provenance, not aliasing

The three-domain cycle removes the interpretation that Candidate A's research value lies in:

```text
alpha_V(x) = alpha_V(y)
```

That equality is elementary and can arise for many ordinary reasons.

The narrow residual is now:

> **Under what evidence conditions can a system, before possessing the later correct refinement, establish that a specific task-relevant discriminator is absent from the relied-upon decision boundary rather than merely unused, noisy, miscalibrated, or hidden elsewhere in the system?**

The evidence obligations now include at least:

```text
pre-refinement timing;
independent source for the task pressure;
independent evidence that the discriminator is unavailable at the relevant boundary;
explicit search for existing/unused channels;
explicit localization against measurement/model error;
weak conclusion that does not infer a repair.
```

## 5. Degrees of freedom removed

The cycle permanently removes these shortcuts:

```text
post-refinement knowledge may not serve as earlier certificate provenance;
policy omission may not be redescribed as representation absence;
measurement aliasing may not be redescribed as representation inadequacy;
conservative fallback availability refutes any stronger “no action exists” claim;
one surviving domain may not be promoted into a generic theorem family.
```

## 6. QX status after QX-4A

```text
QX-1 problem kernel: COMPLETE
QX-2 prior-art kill: COMPLETE
QX-3 candidate preregistration: COMPLETE
QX-4A domain set: COMPLETE
Candidate A: SURVIVES WITH EXACT RESIDUAL / MECHANISM-SPECIFIC ONLY
Candidate B: PREREGISTERED / NOT YET DOMAIN-TESTED
Generic QX object: NOT EARNED
QX Lean: NO
```

## 7. Next-step discipline

This verdict does **not** start Candidate B automatically.

The next permitted research actions are independent:

```text
QC-3A source-viability + native-decomposition acquisition;
QX-4B only in a later cycle, after Candidate A's verdict is frozen.
```

No QX/QC bridge is inferred from the Web-PKI case.
